{
  "name": "promptfrenzy-ai-directory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Badge-required AI tools directory — see README.md",
  "scripts": {
    "verify:pr": "node scripts/verify-badges.mjs --pr",
    "verify:all": "node scripts/verify-badges.mjs --all-active",
    "verify:recent": "node scripts/verify-badges.mjs --recent-7d",
    "test:host-class": "node scripts/host-class.test.mjs",
    "test:removal-policy": "node scripts/removal-policy.test.mjs",
    "sweep:removed": "node scripts/verify-badges.mjs --sweep-removed",
    "sweep:dry-run": "node scripts/verify-badges.mjs --sweep-removed --dry-run"
  },
  "dependencies": {
    "yaml": "^2.5.0",
    "linkedom": "^0.18.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
